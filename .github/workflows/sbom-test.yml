name: SBOM Test
on:
  push:
    branches:
      - feat/sbom
jobs:
  generate-sboms:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Install cdxgen
        run: npm install -g @cyclonedx/cdxgen

      - name: Create output directory
        run: mkdir -p output

      - name: Generate SBOM for scripts
        run: cdxgen -r ./scripts -o output/bom-card.json

      - name: Generate SBOM for website
        run: cdxgen -r ./cornucopia.owasp.org -o output/bom-website.json

      - name: Generate SBOM for engine
        run: cdxgen -r ./copi.owasp.org -o output/bom-engine.json

      - uses: actions/upload-artifact@v4
        with:
          name: sboms
          path: output/*.json